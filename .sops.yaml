# SOPS configuration — https://github.com/getsops/sops
#
# Replace the placeholder below with your actual age public key.
# Generate a keypair:  age-keygen -o ~/.config/sops/age/keys.txt
# The public key is printed to stdout and also saved in the file.
#
# For team access, add multiple recipients separated by commas.
creation_rules:
  # Root .env (server secrets)
  - path_regex: \.env(\.encrypted)?$
    age: "age1a8ngt0je2qlflhr8rcrjc6qyw95l7ffzmkmwaqt930ggjsz4spnq9c5440"
