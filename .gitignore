# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
.next
build
out

# Environment (plaintext — never commit)
.env
.env.local
.env.*.local
# SOPS encrypted files ARE safe to commit — do not ignore them
!*.env.encrypted
!.env.encrypted

# Turbo
.turbo

# Logs
*.log
loki-data/
grafana-data/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# Drizzle
drizzle/meta

# Test coverage
coverage

# Seed files (contain predictable IDs — never use in production)
seed-*.sql

# Claude Code machine-local settings (never commit)
.claude/settings.local.json
